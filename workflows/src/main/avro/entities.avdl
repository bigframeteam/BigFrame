@namespace("bigframe.avro")
protocol SparkParquetAvro_E {
	import idl "url.avdl";
	import idl "user_mention.avdl";

	record Entities {
		union {null, array<string>} hashtags;
		union {null, array<Url>} urls;
		union {null, array<User_Mention>} user_mentions;
	}

}