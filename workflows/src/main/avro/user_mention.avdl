@namespace("bigframe.avro")
protocol SparkParquetAvro_UM {

	record User_Mention {
		union {null, long} id;
		union {null, string} id_str;
		union {null, array<int>} indices;
		union {null, string} name;
		union {null, string} screen_name;
	}

}