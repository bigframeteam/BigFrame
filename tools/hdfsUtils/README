####################################################
#
#	These scripts are used to copy data
#   between local file systems and HDFS.
#   
#   Script annotated with cloudera is dedicated for
#   Cloudera HDFS distribution.
#
####################################################

copyHdfsFile2Hosts.sh: copy files in HDFS to a set of hosts 
copyHostsFile2HDFS.sh: parallel copy files in hosts to HDFS
copyTPCDSHdfs2Hosts.sh: copy TPCDS files in HDFS to a set of hosts
copyHostsFile2TPCDSHdfs.sh: parallel copy TPCDS files in hosts to HDFS

copyHostsFile2HDFS-cloudera.sh: cloudera specific version
copyHostsFile2TPCDSHdfs-cloudera.sh: cloudera specific version

run ./script.sh can show its usage
 
